# chezmoi の適用

このリポジトリの chezmoi 適用ルールは、このファイルを正本とする。スキルを使わない設定変更にも適用する。

- エージェントは source の編集と読み取り専用の検証を担当する。`chezmoi diff`、`chezmoi source-path`、`chezmoi managed`、テンプレートの確認は実行してよい。
- `chezmoi apply`、`chezmoi update` と同等の home への適用はユーザーが実行する。エージェントは実行しない。
- home の管理対象ファイルを直接編集したり、適用用スクリプトを直接実行したりして適用を代替しない。
- 実施可能な source の編集・検証を終えてから、変更内容、検証結果、ユーザーが実行する適用コマンドを提示する。適用前は「source 更新済み」と「実環境への反映済み」を区別し、適用後にしか確認できない項目は未確認として報告する。
- ユーザー操作を前提とする後続作業は、その完了を確認してから進める。コミットはユーザーが行う。

## スキル固有の運用

- スキルはこの共通ルールを参照し、適用のタイミング、前後の検証、ツール固有の実行分担を記載する。共通ルールの本文を重複定義しない。
- スキル固有の追加制約は、その理由と対象操作をスキルに記載する。
- 共通ルールを緩める例外が必要な場合は、スキルだけで上書きせず、このファイルに対象・理由・実行者を限定して定義する。現在、そのような例外はない。
